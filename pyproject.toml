[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "qIT_Auto_Analyser"
version = "0.1.0"
description = "An auto analyser"

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}
